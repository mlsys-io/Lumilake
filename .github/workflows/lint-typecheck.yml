name: Lint / Format / Typecheck

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

# Runner lane: GitHub-hosted for pull_request (fork-safe), nus-lumilake for a
# write-gated push, kill-switched via vars.NUS_RUNNERS. Full rationale in
# CONTRIBUTING.md#runner-lanes.
jobs:
  lint:
    name: Lint + typecheck
    runs-on: ${{ github.event_name == 'push' && vars.NUS_RUNNERS == 'on' && 'nus-lumilake' || 'ubuntu-latest' }}

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1  # v7.0.1
        with:
          persist-credentials: false

      - name: Set up uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d  # v10.0.1
        with:
          version: "0.11.26"
          python-version: "3.12"

      - name: Install dependencies
        # --all-extras: mypy needs every workspace wheel for cross-imports.
        run: uv sync --frozen --all-extras --all-groups

      - name: Run pre-commit hooks
        run: uv run pre-commit run --all-files --show-diff-on-failure
